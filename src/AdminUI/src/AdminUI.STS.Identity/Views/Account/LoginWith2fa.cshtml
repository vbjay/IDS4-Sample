@inject IViewLocalizer Localizer
@inject Microsoft.AspNetCore.Antiforgery.IAntiforgery Xsrf

@using Microsoft.AspNetCore.Mvc.Localization
@model AdminUI.STS.Identity.ViewModels.Account.LoginWith2faViewModel
@{
    ViewData["Title"] = Localizer["Title"];
}
@functions{
    string GetAntiXsrfRequestToken()
    {
        return Xsrf.GetAndStoreTokens(this.Context).RequestToken;
    }
}

@await Html.PartialAsync("_ValidationSummary")

<h2>@ViewData["Title"]</h2>
<hr />
<p>@Localizer["Info"]</p>

<div class="row">
    <div class="col-md-4">
        <form method="post" asp-route-returnUrl="@Model.ReturnUrl">
            <input asp-for="RememberMe" type="hidden" />
            <input asp-for="ReturnUrl" type="hidden" />
            <div class="form-group">
                <label asp-for="TwoFactorCode">@Localizer["Code"]</label>
                <input asp-for="TwoFactorCode" class="form-control" autocomplete="off" autofocus />
                <span asp-validation-for="TwoFactorCode" class="text-danger"></span>
            </div>
            <div class="form-group">
                <div class="checkbox">
                    <div>
                        @Localizer["Remember"]
                    </div>
                    <toggle-button>
                        <input asp-for="RememberMachine" />
                    </toggle-button>
                </div>
            </div>
            <div class="form-group">
                <button type="submit" class="btn btn-primary">@Localizer["LogIn"]</button>
            </div>
        </form>
    </div>
    <div class="col-md-4">
        <h1>FIDO2</h1>
        <div>
            <p>Authenticate using a FIDO2 supported security key.</p>
        </div>
        <div class="notification is-danger" style="display:none">
            Please note: Your browser does not seem to support WebAuthn yet. <a href="https://caniuse.com/#search=webauthn" target="_blank">Supported browsers</a>
        </div>

        <form action="/LoginFido2Mfa" method="post">
            <input type="hidden" id="RequestVerificationToken" name="RequestVerificationToken" value="@GetAntiXsrfRequestToken()">
            <div class="field">
                <div class="control">
                    <button id="signin" class="btn btn-primary">2FA with FIDO2 key</button>
                </div>
            </div>
        </form>


        <div id="fido2logindisplay"></div>
        <div style="display:none" id="fido2TapKeyToLogin">Follow prompts to complete Fido2 key authentication.</div>
        <div style="display:none" id="fido2CouldNotVerifyAssertion">Error validating key</div>
        <div style="display:none" id="fido2ReturnUrl">@Model.ReturnUrl</div>


    </div>
</div>






@section Scripts{
    <script src="~/js/helpers.js" asp-append-version="true"></script>
    <script src="~/js/instant.js" asp-append-version="true"></script>
    <script src="~/js/mfa.login.js" asp-append-version="true"></script>
}
<p>
    @Localizer["NoAuthenticatorDevice"]
    <a asp-action="LoginWithRecoveryCode" asp-route-returnUrl="@Model.ReturnUrl">@Localizer["LoginWithRecoveryCode"]</a>
</p>








